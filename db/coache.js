/*const mongoose = require('mongoose');

const coache = mongoose.model('coache', new mongoose.Schema({
    name: {
      type: String,
      required: true,
      minlength: 5,
      maxlength: 50
    },
    age: {
      type: String,
    },
    position: {
      type: String,
      required: true,
    }
  }));

  module.exports= coache;
  */